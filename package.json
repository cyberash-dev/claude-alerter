{
  "name": "claude-alerter",
  "version": "1.1.1",
  "description": "Cross-platform notifier for Claude Code hooks: sound and optional haptic alerts that repeat until the terminal is focused.",
  "license": "MIT",
  "author": "cyberash <cyberash-dev@yandex.com>",
  "homepage": "https://github.com/cyberash-dev/claude-alerter#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cyberash-dev/claude-alerter.git"
  },
  "bugs": {
    "url": "https://github.com/cyberash-dev/claude-alerter/issues"
  },
  "keywords": [
    "claude",
    "claude-code",
    "hooks",
    "notification",
    "sound",
    "haptic",
    "notifier",
    "cli",
    "cross-platform"
  ],
  "type": "commonjs",
  "bin": {
    "claude-alerter": "dist/src/cli.js"
  },
  "files": [
    "dist/",
    "sounds/",
    "config.example.json"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test \"dist/test/**/*.test.js\"",
    "gen-sounds": "tsc && node dist/tools/gen-sounds.js",
    "setup": "tsc && node dist/src/cli.js install",
    "prepare": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
